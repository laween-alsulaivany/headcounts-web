# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Required: Flask secret key for session cookies and CSRF tokens.
# You can generate it with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key
